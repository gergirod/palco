# Palco — variables de entorno (copiá a .env.local para dev, y cargá en Vercel para prod)

# Supabase del proyecto Palco (login magic-link + lectura de datasets).
# Por ahora podés apuntar al mismo Supabase de eco; lo ideal es un proyecto nuevo.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
